# Learnings

Durable lessons the orchestrator should remember across sessions. Loaded into context at session start,
so keep entries short and high-value. Append during the completion mandate's vault-learning step.

Format: `- [YYYY-MM-DD] <area>: <lesson> (why it matters)`

<!-- Examples — replace with your own as you go:
- [2026-06-15] auth: refresh tokens are rotated server-side; never cache them client-side (security).
- [2026-06-15] build: `pnpm` workspace requires `--filter` for per-package tests (saves time).
-->
