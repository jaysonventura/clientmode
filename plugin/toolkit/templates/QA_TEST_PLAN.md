# QA Test Plan

Generated: {{generatedAt}}

Each requirement maps to at least one verification. Run verifications via `cdt-verify -- <command>` so the
result is recorded as trusted evidence.

| Req ID | Requirement (cited) | Test approach | Status |
|---|---|---|---|
{{rows}}
