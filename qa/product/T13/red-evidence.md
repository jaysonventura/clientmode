# AT-013 — red evidence

Gate: `tests/tasks/T13.test.ts`, executor `tests/scenarios/T13.scenarios.ts`.
Command: `node --import tsx --test tests/tasks/T13.test.ts`.
Task: Ordinary-language intake and AI-maintained project memory.

## Mutations proving the assertions are load-bearing

Each mutation removes one named production control, the gate is run, and the mutation is
reverted. Every row below was executed; the full cross-gate table, including the mutations no
gate detected, is in `qa/product/MUTATION-MATRIX.md`.

| # | Mutation | Result |
|---|----------|--------|
| `K22c` | project memory recall returns every project's facts | **RED** |
| `K29` | client exclusions dropped from the interpreted requirements (`packages/core/src/intake.ts`) | **RED** |

## Provenance of this file

The original write-up for this gate was destroyed before it was ever committed: `Evidence.open()`
used to empty `qa/product/<task>/` at the start of every run, which deleted the authored
`red-evidence.md` beside the generated JSON each time the suite ran. That bug was found and fixed
in T24. This file was **regenerated by re-running the mutations above**, not reconstructed from
memory: the results here are from the recorded runs in `qa/product/mutation-matrix.json`.


## Round 2 — a corpus of what clients actually write

`fixtures/intake/briefs.ts` holds twenty briefs of the kind a small business sends: mixed English
and Filipino, no structure, the important constraint in the last line. Each declares what the
reader must produce, and the gate requires all twenty.

The column that matters most is `must_not_include`. It catches a negated phrase being read as a
feature — the client's meaning coming out backwards, which is worse than missing it, because the
client then has to notice and undo it.

The corpus scored **16 of 20** on the build that had just passed 33 green gates. Three were real
defects in the reader:

| Brief | What went wrong |
|---|---|
| `no_online_payment` | "customer pay when deliver" was not read as cash on delivery. Few clients write the literal phrase; missing it means proposing the online payment they said they did not want. |
| `quantity_bug` | "kapag ginawa kong zero yung quantity" produced no cart requirement. A client reporting a quantity problem is describing the cart, whatever they call it. |
| `refund_permission` | "staff should be able to refund an order" raised no question. A refund is money leaving the account, and that is the client's decision every time. |

The fourth was **my expectation, not the code**: I had marked `delivery` as an inversion in
"walang delivery fee for now, libre muna ang hatid". The client does deliver — they just do not
charge for it, and `no-delivery-fee` is what carries that. The corpus was corrected and the reason
recorded beside the brief.

| # | Mutation | Result | Observation |
|---|----------|--------|-------------|
| `C1` | cash-on-delivery narrowed back to the literal phrase | **RED** | `excluded_business_rules_preserved` |
| `C2` | a quantity report is no longer a cart requirement | **RED** | `excluded_business_rules_preserved` |
| `C3` | a refund raises no money question | **RED** | `excluded_business_rules_preserved` |

`C4` — removing the "all twenty must pass" clause — is not a meaningful mutation and is recorded
as such: weakening an assertion never fails a build. `C1` is what proves the clause bites, because
it makes the corpus fail and the gate goes red.


## Round 3 — the eight briefs that were actually hard

Once the reader was fixed the first twenty all passed, which meant the corpus had stopped telling
us anything. Eight harder briefs were added: negation that lands in the next sentence, a
constraint phrased as a question, a contradiction inside one message, code-switching mid-sentence,
polite padding around a single requirement, a complaint that is not a request, and a pure question
that should produce no requirements at all.

**25 of 28.** Three failed, and two of them for the same reason:

| Brief | What went wrong |
|---|---|
| `negation_next_sentence` | "Wag na yung online payment ha" — `wag` was not among the words the exclusion patterns recognised as a refusal, though it is the most common one in the language the client wrote in. |
| `exclusion_as_question` | "Kailangan pa ba ng account? **Ayaw ko sana.**" |
| `contradiction_in_one_message` | "Kailangan namin ng online payment... **Pero wag muna**, cash on delivery lang." |

The last two are the same shape and the interesting one: **people do not repeat the noun when
they reject something they have just named.** The refusal lands in its own sentence and refers
back, and a reader that only looks inside one sentence misses the most important word in the
message. `trailingRejections` attaches a bare refusal to the nearest thing named in the two
sentences before it.

| # | Mutation | Result | Observation |
|---|----------|--------|-------------|
| `R1` | a refusal in the next sentence is no longer attached to what it refuses | **RED** | `excluded_business_rules_preserved` |
| `R2` | the refusal only looks inside its own sentence | **RED** | `excluded_business_rules_preserved` |
| `R3` | `wag` and `ayaw` are no longer refusal words | **RED** | `excluded_business_rules_preserved` |
