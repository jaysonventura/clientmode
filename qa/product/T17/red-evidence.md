# AT-017 — red evidence

Gate: `tests/tasks/T17.test.ts`, executor `tests/scenarios/T17.scenarios.ts`.
Command: `node --import tsx --test tests/tasks/T17.test.ts`.
Task: Dual native plugin distributions and safe installation.

## Mutations proving the assertions are load-bearing

Each mutation removes one named production control, the gate is run, and the mutation is
reverted. Every row below was executed; the full cross-gate table, including the mutations no
gate detected, is in `qa/product/MUTATION-MATRIX.md`.

| # | Mutation | Result |
|---|----------|--------|
| `K21b` | uninstall leaves its files behind (`packages/packaging/src/install.ts`) | **RED** |
| `K40` | a shipped skill carries a path back into the build tree (`skills/handoff/SKILL.md`) | **RED** |

## Provenance of this file

The original write-up for this gate was destroyed before it was ever committed: `Evidence.open()`
used to empty `qa/product/<task>/` at the start of every run, which deleted the authored
`red-evidence.md` beside the generated JSON each time the suite ran. That bug was found and fixed
in T24. This file was **regenerated by re-running the mutations above**, not reconstructed from
memory: the results here are from the recorded runs in `qa/product/mutation-matrix.json`.


## Round 3 — an install that does not need the checkout

`cm` shipped with the developer's own path baked into the launcher. A client's machine has no
`/Users/<someone>/Documents/clientmode`, and a tool that points at one stops working the moment
a folder moves.

The gate now builds the portable toolkit the way an install ships it — one bundled entry point
plus the files the controller reads at runtime — and then runs it **from a directory that is not
the checkout**, asserting the bundle contains no reference to the checkout at all.

Three things were wrong, and none of them showed up in a repo-relative run:

1. **Six runtime file reads resolved `../../../..` from the module.** Right in a checkout, wrong
   in a bundle, which is how a tool ends up working only on the machine it was written on. They
   now share one resolver that walks up until it finds `contracts/domain.schema.json`, so the
   same code finds its files in either layout. Two of the six — `contracts/state-machine.json`
   and `contracts/storage/release.sql` — were only found by running the packaged bundle; reading
   the source had missed them.
2. **`esbuild` was imported at module load** through the console bundler, so a packaged install
   without it could not start *any* command, including `doctor`. The console bundler is now
   loaded only when `cm open` needs it, and its absence is reported as a capability gap.
3. **The runtime files were not travelling with the package.** Now `contracts/`, `adapters/` and
   `skills/` ship beside the bundle.

| # | Mutation | Result | What it proves |
|---|----------|--------|----------------|
| `P1` | the toolkit root assumed four directories up again | **RED** | the resolver, not the guess, is what makes it portable |
| `P2` | the runtime files no longer travel with the package | **RED** | a bundle without its schemas is not an install |
| `P3b` | the console bundler bundled in, making `esbuild` a load-time import | **RED** | one optional dependency must not stop every command |

The bundle is 459 KB and runs `doctor`, `status` and `handoff` from an unrelated directory.


## Round 4 — `cm install` ships the portable toolkit

Round 3 proved a portable install was *possible*. `cm install` still did not produce one: the
launcher on the machine pointed at the developer's checkout.

`cm install` now builds the portable toolkit into `$CM_HOME/toolkit` and writes a launcher that
names it, so the CLI never has to guess where its files are and never depends on where the source
was when it was installed. The gate runs that launcher from an unrelated directory and asserts it
contains no reference to the checkout.

| # | Mutation | Result | What it proves |
|---|----------|--------|----------------|
| `Q1` | the launcher is not made executable | **RED** | `mode` on `writeFileSync` only applies when the file is created; an existing launcher kept whatever it had, so `chmod` is what makes a reinstall work |
| `Q2` | the launcher points at the checkout instead of the installed toolkit | **RED** | the whole point of the round |
| `Q3` | the launcher names no toolkit root at all | **RED** | without it the bundle resolves its files from wherever it was invoked |
| `Q4` | repeated install and uninstall leaves a growing gap | **RED** | each cycle used to push the client's own content further down the page |

`Q4` was green at first: the gate did one install/uninstall cycle and `deactivate`'s own trim
hid the accumulation. It now runs three more cycles and requires the file to come back
byte-identical with no run of blank lines introduced — which is what actually happens on a
client's machine when they reinstall.

## Round 9 — `cm open` did not work on any packaged install

Found by running the installed launcher outside the checkout, which is what a client has:

```
$ cm run --root . --request-file brief.txt
run run_c3f7914b-17b3-4880-8072-407a78537275
$ cm open --root .
the console cannot be built on this install: Cannot find module
  '/Users/viscion/.client-mode/toolkit/console-bundle.js'
Everything else still works; `cm status` and `cm handoff` do not need it.
```

The console was compiled on demand, and the module that compiles it was marked external in the
bundle so `esbuild` would not become a load-time dependency of the CLI. Marking it external
also meant it was never copied, so the console — the client's only window into a run — worked
in the checkout and nowhere else. Every portability assertion in this gate passed while it was
broken, because all of them ran commands that do not need it.

It is now compiled at install time, where the bundler is already in hand, and shipped as
`console/app.js`. The per-run boot data moved to `console-page.ts`, which has no bundler import,
so writing the page costs nothing at open time.

The gate opens a run through the installed launcher from an unrelated directory and asks the
server it starts for the page and the compiled application:

```
"ships_compiled_application": true, "run_exit_code": 0,
"url": "http://127.0.0.1:53989", "page_status": 200, "boot_present": true,
"app_status": 200, "app_bytes": 1089783
```

### Mutation

| # | Mutation | Result |
|---|----------|--------|
| P9 | the toolkit ships without a compiled console, as it did before | **red**, reproducing the original message verbatim |

### What this does not cover

The page is served and the application is fetched. Nothing here renders it: whether the console
is usable in a browser is AT-015's scope, and that gate builds its own bundle rather than the
shipped one.
