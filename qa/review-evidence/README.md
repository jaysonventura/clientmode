# Audit reproduction records

Baseline and failure-first observations from this audit. Failing logs are intentional evidence of the old behavior, not current failures. A test-fixture INSERT argument mismatch was corrected before retaining the Python failure log here. The passing current suite is in `qa/current/`. The OpenAPI dependency retrieval failed, so no full conformance result is claimed. These are reference/contract checks, not product execution.
